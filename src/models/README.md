## Logistic Regression Model:
The baseline Logistic Regression model achieved an accuracy of 61.1%. However, despite a moderate precision (63.1%), the recall for readmitted patients was only 41.8%, indicating that the model failed to identify a substantial proportion of patients who were actually readmitted. Since minimizing missed readmissions is a primary objective in clinical decision support systems, improving recall should be prioritized in subsequent models.

